// This file shows the branded loader while top-level storefront routes are loading.
import { PageLoader } from "@/components/ui/page-loader";

export default function Loading() {
  return <PageLoader />;
}